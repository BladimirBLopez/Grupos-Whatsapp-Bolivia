// api/grupos.js
const fs = require('fs');
const path = require('path');

module.exports = async function handler(req, res) {
  // Configurar CORS
  res.setHeader('Access-Control-Allow-Origin', '*');
  res.setHeader('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE, OPTIONS');
  res.setHeader('Access-Control-Allow-Headers', 'Content-Type');

  if (req.method === 'OPTIONS') {
    return res.status(200).end();
  }

  // Ruta al archivo JSON
  const jsonPath = path.join(process.cwd(), 'data', 'grupos.json');

  // ============================================
  // GET - Obtener todos los grupos
  // ============================================
  if (req.method === 'GET') {
    try {
      console.log('📥 GET /api/grupos');
      const data = JSON.parse(fs.readFileSync(jsonPath, 'utf8'));
      return res.status(200).json(data);
    } catch (error) {
      console.error('❌ Error GET:', error);
      return res.status(500).json({ 
        error: 'Error al obtener grupos', 
        details: error.message 
      });
    }
  }

  // ============================================
  // POST - Crear nuevo grupo
  // ============================================
  if (req.method === 'POST') {
    try {
      console.log('📥 POST /api/grupos');
      console.log('📦 Body:', req.body);
      
      const { grupo } = req.body;
      
      if (!grupo || !grupo.nombre || !grupo.link) {
        return res.status(400).json({ 
          error: 'Datos incompletos: nombre y link son requeridos' 
        });
      }

      let data;
      try {
        data = JSON.parse(fs.readFileSync(jsonPath, 'utf8'));
      } catch (e) {
        data = { grupos: [] };
      }
      
      const nuevoId = data.grupos.length > 0 
        ? Math.max(...data.grupos.map(g => g.id)) + 1 
        : 1;
      
      const nuevoGrupo = {
        id: nuevoId,
        ...grupo,
        plataforma: grupo.plataforma || 'whatsapp',
        fecha: new Date().toISOString()
      };
      
      data.grupos.push(nuevoGrupo);
      fs.writeFileSync(jsonPath, JSON.stringify(data, null, 2));
      console.log('✅ Grupo creado ID:', nuevoId);
      
      return res.status(201).json({ 
        success: true, 
        grupo: nuevoGrupo,
        message: 'Grupo creado exitosamente'
      });
    } catch (error) {
      console.error('❌ Error POST:', error);
      return res.status(500).json({ 
        error: 'Error al crear grupo', 
        details: error.message 
      });
    }
  }

  // ============================================
  // PUT - Actualizar grupo
  // ============================================
  if (req.method === 'PUT') {
    try {
      console.log('📥 PUT /api/grupos');
      console.log('📦 Body:', req.body);
      
      const { id, datos } = req.body;
      
      if (!id) {
        return res.status(400).json({ error: 'ID requerido' });
      }

      let data;
      try {
        data = JSON.parse(fs.readFileSync(jsonPath, 'utf8'));
      } catch (e) {
        data = { grupos: [] };
      }
      
      const index = data.grupos.findIndex(g => g.id === id);
      if (index === -1) {
        return res.status(404).json({ error: 'Grupo no encontrado' });
      }
      
      data.grupos[index] = { ...data.grupos[index], ...datos };
      fs.writeFileSync(jsonPath, JSON.stringify(data, null, 2));
      
      console.log('✅ Grupo actualizado ID:', id);
      
      return res.status(200).json({ 
        success: true, 
        message: 'Grupo actualizado' 
      });
    } catch (error) {
      console.error('❌ Error PUT:', error);
      return res.status(500).json({ 
        error: 'Error al actualizar', 
        details: error.message 
      });
    }
  }

  // ============================================
  // DELETE - Eliminar grupo
  // ============================================
  if (req.method === 'DELETE') {
    try {
      console.log('📥 DELETE /api/grupos');
      console.log('📦 Body:', req.body);
      
      const { id } = req.body;
      
      if (!id) {
        return res.status(400).json({ error: 'ID requerido' });
      }

      let data;
      try {
        data = JSON.parse(fs.readFileSync(jsonPath, 'utf8'));
      } catch (e) {
        data = { grupos: [] };
      }
      
      const grupoExistente = data.grupos.find(g => g.id === id);
      if (!grupoExistente) {
        return res.status(404).json({ error: 'Grupo no encontrado' });
      }
      
      data.grupos = data.grupos.filter(g => g.id !== id);
      fs.writeFileSync(jsonPath, JSON.stringify(data, null, 2));
      
      console.log('🗑️ Grupo eliminado ID:', id);
      
      return res.status(200).json({ 
        success: true, 
        message: 'Grupo eliminado' 
      });
    } catch (error) {
      console.error('❌ Error DELETE:', error);
      return res.status(500).json({ 
        error: 'Error al eliminar', 
        details: error.message 
      });
    }
  }

  return res.status(405).json({ error: 'Método no permitido' });
};